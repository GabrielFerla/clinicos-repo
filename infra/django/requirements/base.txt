# Dependências base do ClinicOS (runtime).
# Pacotes consenso para Sprint 1; novas dependências de domínio
# (chatbot, embeddings, etc.) entram em sprints posteriores.

Django>=5.1,<5.2
django-environ>=0.11,<1.0
django-tailwind[reload]>=3.8
oracledb>=2.4
redis>=5.0
celery>=5.4,<6.0
gunicorn>=22.0
cryptography>=42.0

# Chatbot (Sprint 5). O cliente da OpenAI é usado para falar com o **Ollama
# local**, que expõe um endpoint compatível (/v1/chat/completions) — não há
# chamada a serviço pago. Ver docs/CHAT_MVP.md e ADR-0006.
#
# Os embeddings também saem do Ollama, de propósito: `sentence-transformers`
# arrastaria `torch` (~2,3 GB) para dentro desta imagem sem ganho de qualidade
# relevante para uma FAQ de clínica.
openai>=1.50,<2.0
