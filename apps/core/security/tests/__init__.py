"""Testes unitários dos helpers de segurança (S1-15).

Cobrem ``apps.core.security.crypto``, ``apps.core.security.hash`` e
``apps.core.security.keys`` — todos puros, sem dependência de Django/Oracle.
Por serem código de segurança, a meta de cobertura é 100% (acima do mínimo
de 70% definido no CLAUDE.md para módulos de domínio).
"""
