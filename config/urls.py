"""URL configuration raiz do ClinicOS.

Esqueleto vazio — as sprints seguintes vão registrar as rotas dos apps:
- `core`, `agenda`, `prontuario`, `chatbot`, `crm`, `site_publico` (S1-5+).
"""

from django.urls import URLPattern, URLResolver

urlpatterns: list[URLPattern | URLResolver] = []
